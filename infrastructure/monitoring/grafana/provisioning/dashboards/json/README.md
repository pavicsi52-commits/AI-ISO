# Grafana Dashboard JSON

Exported Grafana dashboard JSON files placed here are auto-provisioned on
Grafana startup (see `../dashboards.yml`). Empty for now — Grafana is
temporary per `docs/003_Technology_Stack_Master.md.txt` and will be replaced
by a native AI-IOS dashboard in a future phase.
