"""Starter templates for new plugins ("Generate SDK examples and plugin templates").

``plugin_template.py`` and ``manifest_template.yaml`` are copy-paste
starting points for a new plugin author -- deliberately not imported
here, since ``MyPlugin`` is a placeholder name, not part of this
package's own API.
"""

from __future__ import annotations
